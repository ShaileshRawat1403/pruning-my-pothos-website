<figure class="diagram diagram-scroll">
  <svg
    viewBox="0 0 900 240"
    role="img"
    aria-labelledby="layers-title layers-desc"
    style="width: 100%; height: auto; display: block;"
  >
    <title id="layers-title">Socio-technical layers</title>
    <desc id="layers-desc">
      Three stacked layers labeled Human, Machine, and Interconnectivity with a
      feedback loop.
    </desc>
    <rect x="60" y="30" width="780" height="50" rx="12" stroke="currentColor" stroke-width="2" fill="none" />
    <text x="90" y="62" font-size="12" fill="currentColor" font-family="var(--font-mono)">Human layer: strategic intent</text>

    <rect x="60" y="95" width="780" height="50" rx="12" stroke="currentColor" stroke-width="2" fill="none" />
    <text x="90" y="127" font-size="12" fill="currentColor" font-family="var(--font-mono)">Machine layer: intent translation &amp; execution</text>

    <rect x="60" y="160" width="780" height="50" rx="12" stroke="currentColor" stroke-width="2" fill="none" />
    <text x="90" y="192" font-size="12" fill="currentColor" font-family="var(--font-mono)">Interconnectivity: feedback &amp; clarification</text>

    <path d="M 820 185 C 880 145, 880 95, 820 55" stroke="var(--color-accent)" stroke-width="2" fill="none" stroke-linecap="round" />
    <path d="M 826 66 L 816 56 L 830 54 Z" fill="var(--color-accent)" />
  </svg>
  <figcaption>The system is the full human‑machine loop, not just the model.</figcaption>
</figure>
