---
import Layout from '../layouts/SystemsLayout.astro';
import ToolStatusBadge from '../components/ToolStatusBadge.astro';

const toolCategories = [
  {
    id: 'prompt-context',
    name: 'Prompt & Context',
    desc: 'Utilities to structure raw inputs and package workspace context for LLMs.'
  },
  {
    id: 'harness-eval',
    name: 'Harness & Evaluation',
    desc: 'Harness templates to run local evaluations and score model performance.'
  },
  {
    id: 'workflow-loop',
    name: 'Workflow & Loop',
    desc: 'Orchestrators to map loops and output human-in-the-loop review gates.'
  },
  {
    id: 'docs-assets',
    name: 'Docs & Assets',
    desc: 'Documentation parsers and diagram generators to keep schemas in sync.'
  }
];

const featuredTools = [
  {
    id: 'prompt-to-json',
    category: 'prompt-context',
    status: 'live' as const,
    title: 'Prompt ➔ JSON',
    desc: 'Convert unstructured prose prompts into production-ready JSON schemas.',
    engine: 'ToolSmith',
    mode: 'Browser prototype',
    contract: 'Tessera-compatible',
    future: 'Tessera JobPack',
    output: 'JSON Schema + Example',
    install: 'tesserakit-prompts',
    href: '/tools/prompt-to-json/'
  },
  {
    id: 'workflow-to-diagram',
    category: 'workflow-loop',
    status: 'live' as const,
    title: 'Workflow ➔ Diagram',
    desc: 'Generate interactive Mermaid flowcharts and schemas from text workflows.',
    engine: 'ToolSmith',
    mode: 'Browser prototype',
    contract: 'Tessera-compatible',
    future: 'Tessera JobPack',
    output: 'Mermaid + JSON Graph',
    install: 'tesserakit-recipes',
    href: '/tools/workflow-to-diagram/'
  },
  {
    id: 'repo-context-pack',
    category: 'prompt-context',
    status: 'prototype' as const,
    title: 'Repo ➔ Context Pack',
    desc: 'Gather and compress repository structures into a single XML pack for LLM ingestion.',
    engine: 'ToolSmith',
    mode: 'Manual input',
    contract: 'Tessera-compatible',
    future: 'Tessera JobPack',
    output: 'XML Context Pack',
    install: 'tesserakit-repo',
    href: '/tools/repo-context-pack/'
  },
  {
    id: 'csv-to-eval',
    category: 'harness-eval',
    status: 'prototype' as const,
    title: 'CSV ➔ Eval Harness',
    desc: 'Convert raw CSV outputs into structured evaluation datasets and rubrics.',
    engine: 'ToolSmith',
    mode: 'Browser prototype',
    contract: 'Tessera-compatible',
    future: 'Tessera JobPack',
    output: 'JSONL + Rubric draft',
    install: 'tesserakit-evals',
    href: '/tools/csv-to-eval/'
  },
  {
    id: 'change-to-checklist',
    category: 'workflow-loop',
    status: 'prototype' as const,
    title: 'Change Request ➔ HITL Checklist',
    desc: 'Analyze codebase change requests to generate human-in-the-loop review gates.',
    engine: 'ToolSmith',
    mode: 'Browser prototype',
    contract: 'Tessera-compatible',
    future: 'Flowright',
    output: 'Review gates + checklist',
    install: 'tesserakit-core',
    href: '/tools/change-to-checklist/'
  },
  {
    id: 'notes-to-brief',
    category: 'docs-assets',
    status: 'prototype' as const,
    title: 'Code/Notes ➔ PDF Brief',
    desc: 'Transform raw dev notes and diagrams into print-ready documentation briefs.',
    engine: 'ToolSmith',
    mode: 'Browser prototype',
    contract: 'Tessera-compatible',
    future: 'Content Engine',
    output: 'Structured markdown',
    install: 'tesserakit-core',
    href: '/tools/notes-to-brief/'
  }
];

const stackLayers = [
  {
    id: 'governance',
    name: 'Governance Layer',
    solves: 'Aligns execution with organizational policy.',
    breaks: 'Agents run unguided, executing unsafe or non-compliant operations.',
    tool: 'Change Request ➔ HITL Checklist'
  },
  {
    id: 'loop',
    name: 'Execution Loops',
    solves: 'Establishes checks and balance gates.',
    breaks: 'Silent failures and hallucinations escape into production.',
    tool: 'CSV ➔ Eval Harness'
  },
  {
    id: 'workflow',
    name: 'Workflow Orchestration',
    solves: 'Structured state machines and transitions.',
    breaks: 'Brittle prompts attempt to handle multi-step workflows sequentially.',
    tool: 'Workflow ➔ Diagram'
  },
  {
    id: 'context',
    name: 'Context Architecture',
    solves: 'Provides precise working memory bounds.',
    breaks: 'Irrelevant code bloats context windows, degrading model reasoning.',
    tool: 'Repo ➔ Context Pack'
  },
  {
    id: 'prompt',
    name: 'Prompt Specification',
    solves: 'Contracts for structured inputs and outputs.',
    breaks: 'Fuzzy prompts return unstructured text that crashes JSON parsers.',
    tool: 'Prompt ➔ JSON'
  }
];
---

<Layout 
  title="Sans Serif Systems | Open Tools for Natural Language Programming"
  description="Sans Serif Systems offers browser-native utilities, visual canvases, and evaluation harness templates to design, govern, and validate AI-assisted developer workflows."
>
  <!-- Glowing blobs -->
  <div class="glow-blob glow-cyan"></div>
  <div class="glow-blob glow-purple"></div>

  <!-- Hero Section -->
  <section class="hero-section">
    <span class="hero-badge">Sans Serif Systems</span>
    <h1 class="hero-title">Open tools and live demos for<br /><span class="gradient-text">natural language programming</span></h1>
    <p class="hero-subtitle">
      Try browser-native utilities now. Local-first CLI packages are planned. Use them to convert fuzzy model outputs into repeatable production execution workflows.
    </p>
    <div class="hero-ctas">
      <a href="#tools-directory" class="btn-premium btn-primary">Explore Tools</a>
      <a href="/sentiments/" class="btn-premium btn-secondary">Enter Sans Serif Sentiments</a>
    </div>
  </section>

  <!-- Two Paths Grid -->
  <section class="two-paths-section card-glass" style="max-width: 900px; margin: 0 auto 3rem auto; padding: 2.2rem; display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; text-align: left; position: relative; z-index: 10;">
    <div style="border-right: 1px solid var(--card-border); padding-right: 2rem;">
      <h4 style="font-family: var(--font-heading); color: var(--accent-cyan); margin-bottom: 0.5rem; font-size: 1.1rem; text-transform: uppercase; letter-spacing: 0.05em;">Systems</h4>
      <p style="color: var(--text-secondary); font-size: 0.92rem; line-height: 1.6;">
        Tools, interactive canvases, workflow utilities, product docs, and live lab logs.
      </p>
    </div>
    <div>
      <h4 style="font-family: var(--font-heading); color: var(--accent-purple); margin-bottom: 0.5rem; font-size: 1.1rem; text-transform: uppercase; letter-spacing: 0.05em;">Sentiments</h4>
      <p style="color: var(--text-secondary); font-size: 0.92rem; line-height: 1.6;">
        Essays, field notes, notes, archives, and the thinker/tinkerer/translator writing garden.
      </p>
    </div>
  </section>

  <!-- Start Here & Credibility Block -->
  <section class="two-paths-section card-glass" style="max-width: 900px; margin: 0 auto 3rem auto; padding: 2.2rem; display: grid; grid-template-columns: 1.2fr 1fr; gap: 3rem; text-align: left; position: relative; z-index: 10;">
    <div>
      <h4 style="font-family: var(--font-heading); color: var(--accent-cyan); margin-bottom: 0.5rem; font-size: 1.15rem; text-transform: uppercase; letter-spacing: 0.05em;">Start with the layer you need</h4>
      <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5; margin-bottom: 1rem;">
        Navigate the systems workspace depending on your current validation objective:
      </p>
      <table class="comparison-table" style="width: 100%; border-collapse: collapse; font-size: 0.88rem;">
        <thead>
          <tr>
            <th style="text-align: left; padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border); color: var(--accent-cyan); font-weight: 600;">Visitor Need</th>
            <th style="text-align: left; padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border); color: var(--accent-purple); font-weight: 600;">Path</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border); color: var(--text-secondary);">Try a browser utility</td>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border);"><a href="#tools-directory" style="color: var(--accent-cyan); text-decoration: none;">ToolSmith Bench &rarr;</a></td>
          </tr>
          <tr>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border); color: var(--text-secondary);">Understand the system model</td>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border);"><a href="/docs/" style="color: var(--accent-cyan); text-decoration: none;">Docs Catalog &rarr;</a></td>
          </tr>
          <tr>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border); color: var(--text-secondary);">See working proof</td>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border);"><a href="/live-lab/" style="color: var(--accent-cyan); text-decoration: none;">Live Lab Ledger &rarr;</a></td>
          </tr>
          <tr>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border); color: var(--text-secondary);">Use a thinking canvas</td>
            <td style="padding: 0.6rem 0.75rem; border-bottom: 1px solid var(--card-border);"><a href="/canvases/" style="color: var(--accent-cyan); text-decoration: none;">Visual Canvases &rarr;</a></td>
          </tr>
          <tr>
            <td style="padding: 0.6rem 0.75rem; color: var(--text-secondary);">Understand the boundary</td>
            <td style="padding: 0.6rem 0.75rem;"><a href="/docs/public-private-boundary/" style="color: var(--accent-cyan); text-decoration: none;">Public / Private &rarr;</a></td>
          </tr>
        </tbody>
      </table>
    </div>
    
    <div style="display: flex; flex-direction: column; gap: 1.5rem; justify-content: center; border-left: 1px solid var(--card-border); padding-left: 2rem;">
      <div>
        <h5 style="color: var(--accent-cyan); margin-bottom: 0.5rem; text-transform: uppercase; font-size: 0.82rem; letter-spacing: 0.05em; font-family: var(--font-heading);">What this is</h5>
        <ul style="padding-left: 1.1rem; margin: 0; color: var(--text-secondary); font-size: 0.85rem; line-height: 1.5;">
          <li style="margin-bottom: 0.35rem;">A public systems lab for AI workflows</li>
          <li style="margin-bottom: 0.35rem;">A set of browser-native helper utilities</li>
          <li style="margin-bottom: 0.35rem;">A documentation surface for AI-assisted work</li>
          <li style="margin-bottom: 0;">A proof space for governed execution loops</li>
        </ul>
      </div>
      <div>
        <h5 style="color: var(--accent-pink); margin-bottom: 0.5rem; text-transform: uppercase; font-size: 0.82rem; letter-spacing: 0.05em; font-family: var(--font-heading);">What this is not</h5>
        <ul style="padding-left: 1.1rem; margin: 0; color: var(--text-secondary); font-size: 0.85rem; line-height: 1.5;">
          <li style="margin-bottom: 0.35rem;">Not a commercial SaaS platform yet</li>
          <li style="margin-bottom: 0.35rem;">Not autonomous agent deployment</li>
          <li style="margin-bottom: 0.35rem;">Not a live, installable CLI package</li>
          <li style="margin-bottom: 0;">Not a replacement for human review</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Interactive Pain-Point Selector -->
  <section class="selector-section card-glass">
    <h3>What are you trying to build today?</h3>
    <div class="selector-grid">
      <button class="selector-btn" data-target="workflow-to-diagram">
        “I need to turn a text workflow into a visual diagram.”
      </button>
      <button class="selector-btn" data-target="repo-context-pack">
        “I need to package a repository for an AI assistant.”
      </button>
      <button class="selector-btn" data-target="csv-to-eval">
        “I need to generate evaluation rubrics from prompt logs.”
      </button>
      <button class="selector-btn" data-target="prompt-to-json">
        “I need to force prompts to return valid JSON schemas.”
      </button>
    </div>
  </section>

  <!-- Signature Stack Interaction -->
  <section id="signature-stack" class="stack-section">
    <div class="section-header">
      <h2>The Natural Language Programming Stack</h2>
      <p>Interactive architectural layers required to govern agentic workflows without losing control.</p>
    </div>
    
    <div class="stack-container">
      <div class="stack-visual">
        {stackLayers.map((layer) => (
          <div class="stack-layer-row" data-layer={layer.id} tabindex="0" role="button" aria-label={`Inspect ${layer.name}`}>
            <span class="layer-indicator"></span>
            <div class="layer-body">
              <span class="layer-name">{layer.name}</span>
            </div>
          </div>
        ))}
      </div>
      
      <div class="stack-details card-glass">
        <div class="details-pane" data-pane="default">
          <div class="details-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="24" height="24"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          </div>
          <h4>Inspect the Architecture</h4>
          <p>Select or hover any layer in the stack to examine how to control non-deterministic systems in production.</p>
        </div>
        {stackLayers.map((layer) => (
          <div class="details-pane" data-pane={layer.id} hidden>
            <span class="details-meta-tag">LAYER PROTOCOL</span>
            <h4>{layer.name}</h4>
            <div class="details-item">
              <strong>What it solves:</strong>
              <p>{layer.solves}</p>
            </div>
            <div class="details-item">
              <strong style="color: var(--accent-pink);">What breaks without it:</strong>
              <p>{layer.breaks}</p>
            </div>
            <div class="details-item">
              <strong>Supporting Tool:</strong>
              <p style="color: var(--accent-cyan); font-weight: 600;">{layer.tool}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Ecosystem Map Section -->
  <section id="ecosystem-map" class="stack-section" style="margin-bottom: 5rem; border-top: 1px solid var(--card-border); padding-top: 5rem;">
    <div class="section-header">
      <h2>How the Sans Serif Systems stack fits together</h2>
      <p>An overview of the ecosystem layers from browser utilities to planned agent runtimes.</p>
    </div>
    
    <div class="card-glass" style="max-width: 900px; margin: 0 auto; padding: 2.5rem; text-align: left; position: relative; z-index: 10;">
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 2rem;">
        <div style="border-left: 2px solid var(--accent-cyan); padding-left: 1rem;">
          <h4 style="font-family: var(--font-heading); color: var(--accent-cyan); font-size: 1.05rem; margin-bottom: 0.5rem; text-transform: none; letter-spacing: 0;">ToolSmith</h4>
          <span style="font-size: 0.78rem; font-family: var(--font-mono); color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; display: block; margin-bottom: 0.5rem;">Browser Utility Layer</span>
          <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5;">
            Client-side code generators, schema compilers, and query evaluators running natively in the browser.
          </p>
        </div>
        <div style="border-left: 2px solid var(--accent-purple); padding-left: 1rem;">
          <h4 style="font-family: var(--font-heading); color: var(--accent-purple); font-size: 1.05rem; margin-bottom: 0.5rem; text-transform: none; letter-spacing: 0;">Tessera</h4>
          <span style="font-size: 0.78rem; font-family: var(--font-mono); color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; display: block; margin-bottom: 0.5rem;">Job Contract Layer</span>
          <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5;">
            Reusable schemas and converter specifications defining execution payloads for deterministic code validation.
          </p>
        </div>
        <div style="border-left: 2px solid var(--accent-cyan); padding-left: 1rem;">
          <h4 style="font-family: var(--font-heading); color: var(--accent-cyan); font-size: 1.05rem; margin-bottom: 0.5rem; text-transform: none; letter-spacing: 0;">Flowright</h4>
          <span style="font-size: 0.78rem; font-family: var(--font-mono); color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; display: block; margin-bottom: 0.5rem;">Governance & Workflow Layer</span>
          <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5;">
            Structured state transition tables and human-in-the-loop validation checklists that govern runtime decisions.
          </p>
        </div>
        <div style="border-left: 2px solid var(--accent-purple); padding-left: 1rem;">
          <h4 style="font-family: var(--font-heading); color: var(--accent-purple); font-size: 1.05rem; margin-bottom: 0.5rem; text-transform: none; letter-spacing: 0;">BrainBench</h4>
          <span style="font-size: 0.78rem; font-family: var(--font-mono); color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; display: block; margin-bottom: 0.5rem;">SDLC Memory & Control Layer</span>
          <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5;">
            Unified interface managing context parameters, execution checkpoints, and agent handoff protocols.
          </p>
        </div>
        <div style="border-left: 2px solid var(--accent-cyan); padding-left: 1rem;">
          <h4 style="font-family: var(--font-heading); color: var(--accent-cyan); font-size: 1.05rem; margin-bottom: 0.5rem; text-transform: none; letter-spacing: 0;">Hermes</h4>
          <span style="font-size: 0.78rem; font-family: var(--font-mono); color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; display: block; margin-bottom: 0.5rem;">Agent Runtime Operator</span>
          <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5;">
            Deterministic execution nodes and parser-guided agents translating prompts into concrete execution scripts.
          </p>
        </div>
        <div style="border-left: 2px solid var(--accent-purple); padding-left: 1rem;">
          <h4 style="font-family: var(--font-heading); color: var(--accent-purple); font-size: 1.05rem; margin-bottom: 0.5rem; text-transform: none; letter-spacing: 0;">Content Engine</h4>
          <span style="font-size: 0.78rem; font-family: var(--font-mono); color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; display: block; margin-bottom: 0.5rem;">Documentation Workflow Layer</span>
          <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5;">
            Dynamic parser generating documentation specs, validation reports, and interactive visual flow diagrams.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Tools Directory -->
  <section id="tools-directory" class="directory-section">
    <div class="section-header">
      <h2>Featured Tool Bench</h2>
      <p>Six focused utilities designed to solve friction points in AI development loops.</p>
    </div>

    <!-- Product Status Legend -->
    <div class="card-glass" style="max-width: 900px; margin: 0 auto 2rem auto; padding: 1.5rem; text-align: left; font-size: 0.88rem; border-color: rgba(255, 255, 255, 0.05); position: relative; z-index: 10;">
      <h4 style="font-family: var(--font-heading); color: var(--text-primary); margin-bottom: 1rem; font-size: 0.95rem; text-transform: uppercase; letter-spacing: 0.05em;">Product Status Legend</h4>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem;">
        <div>
          <strong style="color: var(--accent-cyan); display: block; margin-bottom: 0.25rem;">Browser Prototype</strong>
          <span style="color: var(--text-secondary); font-size: 0.82rem; line-height: 1.4; display: block;">Runs client-side in the browser with manual input validation.</span>
        </div>
        <div>
          <strong style="color: var(--accent-purple); display: block; margin-bottom: 0.25rem;">Planned CLI Target</strong>
          <span style="color: var(--text-secondary); font-size: 0.82rem; line-height: 1.4; display: block;">Intended future local package path for offline CLI setups.</span>
        </div>
        <div>
          <strong style="color: var(--accent-cyan); display: block; margin-bottom: 0.25rem;">Tessera-compatible</strong>
          <span style="color: var(--text-secondary); font-size: 0.82rem; line-height: 1.4; display: block;">Output schema structure is designed for future job contract reuse.</span>
        </div>
        <div>
          <strong style="color: var(--accent-pink); display: block; margin-bottom: 0.25rem;">Future Wiring</strong>
          <span style="color: var(--text-secondary); font-size: 0.82rem; line-height: 1.4; display: block;">Planned integration lines for secure multi-agent runtimes.</span>
        </div>
      </div>
    </div>

    <!-- Category Filters -->
    <div class="category-filters">
      <button class="filter-btn active" data-filter="all">All Tools</button>
      {toolCategories.map((cat) => (
        <button class="filter-btn" data-filter={cat.id}>{cat.name}</button>
      ))}
    </div>

    <!-- Grid -->
    <div class="tools-grid">
      {featuredTools.map((tool) => (
        <div class="tool-card card-glass" data-category={tool.category} id={`card-${tool.id}`}>
          <div class="card-glow" aria-hidden="true"></div>
          <div class="tool-card-body">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.25rem; flex-wrap: wrap; gap: 0.5rem;">
              <span class="tool-category-badge" style="margin-bottom: 0;">{toolCategories.find(c => c.id === tool.category)?.name}</span>
              <ToolStatusBadge status={tool.status} />
            </div>
            <h3>{tool.title}</h3>
            <p>{tool.desc}</p>
            
            <div class="tool-profile-list" style="margin: 1.25rem 0; border-top: 1px solid var(--card-border); padding-top: 1rem;">
              <div class="profile-row">
                <span class="profile-label">Engine</span>
                <span class="profile-val">{tool.engine}</span>
              </div>
              <div class="profile-row">
                <span class="profile-label">Mode</span>
                <span class="profile-val">{tool.mode}</span>
              </div>
              <div class="profile-row">
                <span class="profile-label">Contract</span>
                <span class="profile-val">{tool.contract}</span>
              </div>
              <div class="profile-row">
                <span class="profile-label">Future Wiring</span>
                <span class="profile-val" style="color: var(--accent-cyan);">{tool.future}</span>
              </div>
            </div>

            <div class="terminal-box" style="opacity: 0.85;">
              <span class="terminal-prompt" style="color: var(--accent-purple);">ℹ</span>
              <span style="color: var(--text-secondary); font-size: 0.82rem; margin-left: 0.4rem; font-family: var(--font-sans);">Planned CLI target: {tool.install}</span>
            </div>

            <div class="card-actions">
              {tool.status === 'live' ? (
                <a href={tool.href} class="card-btn-primary">Try Live in Browser &rarr;</a>
              ) : (
                <a href={tool.href} class="card-btn-primary" style="border-color: rgba(139, 92, 246, 0.3); color: var(--accent-purple); background: transparent;">Open Prototype Sandbox &rarr;</a>
              )}
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Open Source / Collaboration Section -->
  <section class="consulting-cta-section card-glass">
    <div class="consulting-glow"></div>
    <div class="consulting-content">
      <span class="consulting-tag">Open Source & Collaboration</span>
      <h2>Interested in building together?</h2>
      <p>
        These utilities are open-source and shaped by community workflows. If you have suggestions for structured templates, new evaluation configurations, or want to collaborate on natural language programming contracts, let's connect.
      </p>
      <a href="mailto:shailesh.rawat1403@gmail.com" class="btn-premium btn-primary">Connect via Email</a>
    </div>
  </section>
</Layout>

<script is:inline>
  // Interactive Stack Layer Details Hover/Focus sync
  const initStackInteraction = () => {
    const layers = document.querySelectorAll('.stack-layer-row');
    const panes = document.querySelectorAll('.stack-details [data-pane]');

    const showPane = (layerId) => {
      panes.forEach((pane) => {
        pane.hidden = pane.getAttribute('data-pane') !== (layerId || 'default');
      });
      layers.forEach((layer) => {
        layer.classList.toggle('active', layer.getAttribute('data-layer') === layerId);
      });
    };

    layers.forEach((layer) => {
      const id = layer.getAttribute('data-layer');
      layer.addEventListener('mouseenter', () => showPane(id));
      layer.addEventListener('focus', () => showPane(id));
      layer.addEventListener('mouseleave', () => showPane(''));
      layer.addEventListener('blur', () => showPane(''));
    });
  };

  // Pain-point Selector logic
  const initPainPointSelector = () => {
    const btns = document.querySelectorAll('.selector-btn');
    btns.forEach((btn) => {
      btn.addEventListener('click', () => {
        const targetId = btn.getAttribute('data-target');
        const targetCard = document.getElementById(`card-${targetId}`);
        if (targetCard) {
          targetCard.scrollIntoView({ behavior: 'smooth', block: 'center' });
          targetCard.classList.add('highlight-flash');
          setTimeout(() => {
            targetCard.classList.remove('highlight-flash');
          }, 2000);
        }
      });
    });
  };

  // Category filtering
  const initFilters = () => {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const cards = document.querySelectorAll('.tool-card');

    filterBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        filterBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');

        const filter = btn.getAttribute('data-filter');
        cards.forEach((card) => {
          if (filter === 'all' || card.getAttribute('data-category') === filter) {
            card.style.display = 'block';
            card.style.opacity = '0';
            setTimeout(() => {
              card.style.opacity = '1';
            }, 50);
          } else {
            card.style.display = 'none';
          }
        });
      });
    });
  };

  // Copy Terminal Commands helper
  const initCopyCommands = () => {
    const copyBtns = document.querySelectorAll('.copy-terminal-btn');
    copyBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        const cmdText = btn.parentElement.querySelector('.terminal-cmd').innerText;
        navigator.clipboard.writeText(cmdText).then(() => {
          btn.innerText = 'Copied';
          setTimeout(() => {
            btn.innerText = 'Copy';
          }, 1500);
        });
      });
    });
  };

  document.addEventListener('DOMContentLoaded', () => {
    initStackInteraction();
    initPainPointSelector();
    initFilters();
    initCopyCommands();
  });
</script>

<style>
  /* Hero Styles */
  .hero-section {
    text-align: center;
    padding: 7rem 1rem 5.5rem;
    position: relative;
    z-index: 10;
  }

  .hero-badge {
    display: inline-block;
    padding: 0.4rem 1.1rem;
    border-radius: 999px;
    font-size: 0.72rem;
    font-family: var(--font-heading);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 600;
    color: var(--accent-cyan);
    background-color: rgba(0, 242, 254, 0.06);
    border: 1px solid rgba(0, 242, 254, 0.2);
    margin-bottom: 1.75rem;
    box-shadow: var(--shadow-glow-cyan);
  }

  .hero-title {
    font-family: var(--font-heading);
    font-size: clamp(2.4rem, 5.5vw, 4.4rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -1.5px;
    margin-bottom: 1.5rem;
    color: var(--text-primary);
  }

  .hero-subtitle {
    font-size: clamp(1.1rem, 2.2vw, 1.35rem);
    color: var(--text-secondary);
    max-width: 68ch;
    margin: 0 auto 2.5rem;
    line-height: 1.55;
    text-wrap: balance;
  }

  .hero-ctas {
    display: flex;
    gap: 1.25rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Pain-point Selector */
  .selector-section {
    max-width: 900px;
    margin: 1.5rem auto 5rem;
    padding: 2.2rem;
    text-align: center;
    position: relative;
    z-index: 10;
  }

  .selector-section h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 1.5rem;
    letter-spacing: -0.2px;
  }

  .selector-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  .selector-btn {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    padding: 1.1rem 1.4rem;
    font-family: var(--font-sans);
    color: var(--text-secondary);
    font-size: 0.94rem;
    text-align: left;
    cursor: pointer;
    transition: all 0.22s ease;
  }

  .selector-btn:hover {
    border-color: var(--accent-cyan);
    color: var(--text-primary);
    background-color: rgba(0, 242, 254, 0.03);
    transform: translateY(-2px);
    box-shadow: var(--shadow-glow-cyan);
  }

  /* Section Header */
  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-family: var(--font-heading);
    font-size: 2.2rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
    letter-spacing: -0.5px;
  }

  .section-header p {
    color: var(--text-secondary);
    font-size: 1.05rem;
    max-width: 50ch;
    margin: 0 auto;
  }

  /* Architectural Stack */
  .stack-section {
    margin: 6rem auto;
  }

  .stack-container {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 3rem;
    align-items: center;
  }

  .stack-visual {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .stack-layer-row {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.35rem 1.75rem;
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    cursor: pointer;
    outline: none;
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    overflow: hidden;
  }

  .layer-indicator {
    width: 8px;
    height: 8px;
    border-radius: 99px;
    background: var(--text-muted);
    transition: background 0.25s ease, box-shadow 0.25s ease;
  }

  .layer-name {
    font-family: var(--font-heading);
    font-weight: 600;
    font-size: 1.05rem;
    color: var(--text-secondary);
    transition: color 0.25s ease;
  }

  .stack-layer-row:hover,
  .stack-layer-row:focus-visible,
  .stack-layer-row.active {
    background: rgba(99, 102, 241, 0.05);
    border-color: var(--accent-cyan);
    transform: translateX(8px);
  }

  .stack-layer-row:hover .layer-indicator,
  .stack-layer-row:focus-visible .layer-indicator,
  .stack-layer-row.active .layer-indicator {
    background: var(--accent-cyan);
    box-shadow: var(--shadow-glow-cyan);
  }

  .stack-layer-row:hover .layer-name,
  .stack-layer-row:focus-visible .layer-name,
  .stack-layer-row.active .layer-name {
    color: var(--text-primary);
  }

  .stack-details {
    padding: 2.5rem;
    min-height: 310px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .details-pane {
    width: 100%;
    animation: fade-in-up 0.25s ease forwards;
  }

  .details-icon {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: rgba(0, 242, 254, 0.08);
    color: var(--accent-cyan);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    border: 1px solid rgba(0, 242, 254, 0.2);
  }

  .details-meta-tag {
    display: inline-block;
    padding: 0.25rem 0.6rem;
    border-radius: 99px;
    font-size: 0.65rem;
    font-family: var(--font-mono);
    font-weight: 700;
    letter-spacing: 0.05em;
    background: rgba(139, 92, 246, 0.08);
    color: var(--accent-purple);
    border: 1px solid rgba(139, 92, 246, 0.2);
    margin-bottom: 1rem;
    text-transform: uppercase;
  }

  .stack-details h4 {
    font-family: var(--font-heading);
    font-size: 1.45rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 1.25rem;
  }

  .details-item {
    margin-bottom: 1rem;
  }

  .details-item strong {
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    display: block;
    margin-bottom: 0.25rem;
  }

  .details-item p {
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--text-secondary);
  }

  /* Tools Directory grid */
  .directory-section {
    margin: 6rem auto;
  }

  .category-filters {
    display: flex;
    gap: 0.85rem;
    justify-content: center;
    margin-bottom: 3rem;
    flex-wrap: wrap;
  }

  .filter-btn {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    color: var(--text-secondary);
    padding: 0.5rem 1.2rem;
    border-radius: 99px;
    font-family: var(--font-heading);
    font-size: 0.88rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.22s ease;
  }

  .filter-btn:hover,
  .filter-btn.active {
    background: rgba(0, 242, 254, 0.05);
    border-color: var(--accent-cyan);
    color: var(--text-primary);
  }

  .tools-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.75rem;
  }

  .tool-card {
    position: relative;
    overflow: hidden;
    padding: 2.2rem;
    display: flex;
    flex-direction: column;
    height: 100%;
    transition: transform 0.28s cubic-bezier(0.16, 1, 0.3, 1), 
                box-shadow 0.28s cubic-bezier(0.16, 1, 0.3, 1), 
                border-color 0.28s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .tool-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-cyan);
    box-shadow: var(--shadow-glow-cyan);
  }

  .tool-card.highlight-flash {
    animation: flash-highlight-border 2s ease;
  }

  @keyframes flash-highlight-border {
    0%, 100% { border-color: var(--card-border); box-shadow: var(--shadow-premium); }
    30%, 70% { border-color: var(--accent-orange); box-shadow: 0 0 25px rgba(249, 115, 22, 0.3); }
  }

  .card-glow {
    position: absolute;
    inset: -1px;
    border-radius: 18px;
    background: linear-gradient(135deg, var(--accent-cyan), var(--accent-purple));
    opacity: 0.06;
    z-index: -1;
    filter: blur(8px);
  }

  .tool-category-badge {
    display: inline-block;
    padding: 0.25rem 0.55rem;
    border-radius: 6px;
    font-size: 0.65rem;
    font-family: var(--font-mono);
    font-weight: 700;
    letter-spacing: 0.02em;
    background: rgba(255, 255, 255, 0.04);
    color: var(--text-secondary);
    border: 1px solid rgba(255, 255, 255, 0.06);
    margin-bottom: 1.25rem;
    text-transform: uppercase;
  }

  .tool-card h3 {
    font-family: var(--font-heading);
    font-size: 1.35rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
  }

  .tool-card p {
    color: var(--text-secondary);
    font-size: 0.94rem;
    line-height: 1.6;
    margin-bottom: 1.75rem;
    flex-grow: 1;
  }

  .terminal-box {
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    padding: 0.75rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    margin-bottom: 1.75rem;
    position: relative;
  }

  .terminal-prompt {
    color: var(--accent-orange);
    user-select: none;
  }

  .terminal-cmd {
    color: #a7f3d0;
    overflow-x: auto;
    white-space: nowrap;
    width: 100%;
    padding-right: 3rem;
  }

  .copy-terminal-btn {
    position: absolute;
    right: 0.6rem;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: var(--text-secondary);
    font-size: 0.68rem;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    cursor: pointer;
    font-family: var(--font-sans);
    transition: all 0.2s ease;
  }

  .copy-terminal-btn:hover {
    background: rgba(255, 255, 255, 0.12);
    color: var(--text-primary);
  }

  .card-actions {
    margin-top: auto;
  }

  .card-btn-primary {
    display: block;
    text-align: center;
    width: 100%;
    padding: 0.75rem;
    border-radius: 99px;
    font-family: var(--font-heading);
    font-weight: 600;
    font-size: 0.88rem;
    color: var(--accent-cyan);
    border: 1px solid rgba(0, 242, 254, 0.2);
    background-color: rgba(0, 242, 254, 0.02);
    text-decoration: none;
    transition: all 0.22s ease;
  }

  .card-btn-primary:hover {
    background-color: rgba(0, 242, 254, 0.08);
    border-color: var(--accent-cyan);
    box-shadow: var(--shadow-glow-cyan);
  }

  /* Consulting Section */
  .consulting-cta-section {
    position: relative;
    overflow: hidden;
    padding: 4rem 3rem;
    margin: 6rem auto 2rem;
  }

  .consulting-glow {
    position: absolute;
    top: -20%;
    right: -10%;
    width: 350px;
    height: 350px;
    border-radius: 999px;
    background: radial-gradient(circle, var(--accent-purple) 0%, transparent 70%);
    opacity: 0.15;
    z-index: 0;
    filter: blur(80px);
  }

  .consulting-content {
    position: relative;
    z-index: 2;
    max-width: 720px;
  }

  .consulting-tag {
    display: inline-block;
    padding: 0.3rem 0.8rem;
    border-radius: 99px;
    font-size: 0.7rem;
    font-family: var(--font-heading);
    font-weight: 700;
    letter-spacing: 0.05em;
    background: rgba(249, 115, 22, 0.08);
    color: var(--accent-orange);
    border: 1px solid rgba(249, 115, 22, 0.2);
    margin-bottom: 1.5rem;
    text-transform: uppercase;
  }

  .consulting-cta-section h2 {
    font-family: var(--font-heading);
    font-size: 2.2rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 1rem;
    letter-spacing: -0.5px;
  }

  .consulting-cta-section p {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  @keyframes fade-in-up {
    from { opacity: 0; transform: translateY(8px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (max-width: 900px) {
    .hero-section {
      padding: 5rem 1rem 3.5rem;
    }
    .selector-grid {
      grid-template-columns: 1fr;
    }
    .stack-container {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .stack-details {
      min-height: auto;
      padding: 2rem;
    }
    .consulting-cta-section {
      padding: 2.5rem 1.75rem;
    }
  }
</style>
