---
import { getCollection } from 'astro:content';
import Layout from '@/layouts/Layout.astro';
import { sortedByDate } from '@/utils';

const noteEntries = await getCollection('shelf', ({ id }) => id.startsWith('notes/'));
const sortedNotes = sortedByDate(noteEntries);
---

<Layout title="Notes | Shelf">
  <div class="section-head">
    <h1>Notes</h1>
    <p class="subtitle lead">
      Working notes, drafts, and lightweight thinking in motion.
    </p>
  </div>
  <div class="card-grid">
    {sortedNotes.map(entry => (
      <a href={`/shelf/notes/${entry.slug.split('/')[1]}`} class="card">
        <div class="card-img">
          {entry.data.coverUrl ? (
            <img src={entry.data.coverUrl} alt={`Cover image for ${entry.data.title}`} loading="lazy" />
          ) : (
            <span>Cover image</span>
          )}
        </div>
        <div class="card-content">
          <h2 class="card-title">{entry.data.title}</h2>
          <p class="card-description">{entry.data.description}</p>
        </div>
      </a>
    ))}
  </div>
</Layout>
