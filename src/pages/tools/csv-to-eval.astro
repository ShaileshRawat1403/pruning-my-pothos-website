---
import Layout from '../../layouts/SystemsLayout.astro';
---

<Layout title="CSV ➔ Eval Dataset | Sans Serif Systems">
  <div class="glow-blob glow-cyan"></div>
  <div class="glow-blob glow-purple"></div>

  <!-- Breadcrumbs -->
  <div class="breadcrumbs" style="font-family: var(--font-mono); font-size: 0.8rem; margin-bottom: 2rem; color: var(--text-muted);">
    <a href="/tools/" style="color: var(--accent-cyan); text-decoration: none;">Tools</a> / <span style="color: var(--text-primary);">CSV ➔ Eval Dataset</span>
  </div>

  <section class="tool-hero" style="margin-bottom: 3rem;">
    <h1 style="font-family: var(--font-heading); font-size: 2.5rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.5rem;">CSV ➔ Eval Dataset</h1>
    <p class="subtitle" style="font-size: 1.1rem; color: var(--text-secondary); max-width: 700px; line-height: 1.6;">
      Convert raw query-response CSV files into structured JSONL validation datasets and assertion rubrics in-browser.
    </p>
  </section>

  <!-- Sandbox Grid (Try Live) -->
  <section class="sandbox-section" style="margin-bottom: 5rem;">
    <div class="sandbox-container card-glass" style="display: grid; grid-template-columns: 1.1fr 0.9fr; gap: 2rem; border-radius: 12px; overflow: hidden; min-height: 520px;">
      <!-- Input Panel -->
      <div class="sandbox-input-panel" style="padding: 2rem; display: flex; flex-direction: column; gap: 1rem; border-right: 1px solid var(--card-border);">
        <div class="panel-header" style="display: flex; justify-content: space-between; align-items: center; font-family: var(--font-heading); font-size: 0.9rem; font-weight: 600; color: var(--text-primary);">
          <span>1. Paste CSV Data</span>
          <button class="btn-sample" id="load-sample-btn" style="background: rgba(255,255,255,0.03); border: 1px solid var(--card-border); color: var(--text-secondary); padding: 0.3rem 0.65rem; border-radius: 4px; font-size: 0.75rem; cursor: pointer;">Load Sample</button>
        </div>
        <textarea 
          id="csv-input" 
          placeholder="Paste CSV rows here (must include headers on the first line)...
Example:
query,response,expected
'how to build a loop','use Flowright','should mention trigger-action flow'"
          style="width: 100%; flex: 1; min-height: 250px; background: rgba(0,0,0,0.15); border: 1px solid var(--card-border); border-radius: 6px; padding: 1rem; color: var(--text-primary); font-family: var(--font-mono); font-size: 0.82rem; resize: none; outline: none; line-height: 1.5;"
        ></textarea>
        <button class="btn-premium btn-primary" style="width: 100%; padding: 0.85rem; border-radius: 6px; background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--accent-purple) 100%); border: none; color: #fff; font-family: var(--font-heading); font-weight: 600; font-size: 0.95rem; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 0.5rem;" id="generate-btn">
          <span>Compile Dataset</span>
        </button>
      </div>

      <!-- Output Panel -->
      <div class="sandbox-output-panel" style="padding: 2rem; display: flex; flex-direction: column; gap: 1rem;">
        <div class="panel-header" style="display: flex; justify-content: space-between; align-items: center; font-family: var(--font-heading); font-size: 0.9rem; font-weight: 600; color: var(--text-primary);">
          <span>2. Compiled EvalsPack JSONL</span>
          <button class="btn-export" id="copy-json-btn" style="background: rgba(255,255,255,0.03); border: 1px solid var(--card-border); color: var(--accent-cyan); padding: 0.3rem 0.65rem; border-radius: 4px; font-size: 0.75rem; cursor: pointer; display: none;">Copy JSONL</button>
        </div>
        <div class="canvas-viewport" id="canvas-viewport" style="flex: 1; min-height: 250px; background: rgba(0,0,0,0.2); border: 1px solid var(--card-border); border-radius: 6px; display: flex; align-items: center; justify-content: center; position: relative; overflow-y: auto;">
          <div class="canvas-placeholder" id="canvas-placeholder" style="text-align: center; padding: 2rem;">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="48" height="48" style="color: var(--text-muted); margin: 0 auto 1rem;"><polygon points="5 3 19 12 5 21 5 3"/></svg>
            <p style="color: var(--text-secondary); font-size: 0.9rem;">Click Compile Dataset to structure eval outputs</p>
          </div>
          <!-- Code display -->
          <pre class="json-code-box" id="json-code-box" style="width: 100%; height: 100%; margin: 0; padding: 1.25rem; font-family: var(--font-mono); font-size: 0.8rem; line-height: 1.5; color: var(--text-primary); text-align: left; overflow-x: auto;" hidden><code id="json-output"></code></pre>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works Section -->
  <section class="details-section" style="margin-bottom: 5rem; border-top: 1px solid var(--card-border); padding-top: 3rem;">
    <div class="details-grid" style="display: grid; grid-template-columns: 1.2fr 0.8fr; gap: 3rem;">
      <!-- Info Left -->
      <div class="info-content">
        <h2 style="font-family: var(--font-heading); font-size: 1.75rem; color: var(--text-primary); margin-bottom: 1.25rem;">How It Works</h2>
        <p style="color: var(--text-secondary); line-height: 1.7; font-size: 1.05rem; margin-bottom: 1.5rem;">
          This prototype acts as an offline helper compiling raw query logs. It normalizes comma-separated structures, auto-detects input vs expected headers, and maps them to standard assertion objects suitable for validation loops.
        </p>

        <h3 style="font-family: var(--font-heading); font-size: 1.25rem; color: var(--text-primary); margin-top: 2rem; margin-bottom: 0.75rem;">Use Cases</h3>
        <ul style="color: var(--text-secondary); padding-left: 1.2rem; line-height: 1.8; font-size: 1rem; margin-bottom: 2rem;">
          <li>Structuring user execution history CSVs into RAG evaluation tables.</li>
          <li>Generating programmatic validation rubrics from product spreadsheet specs.</li>
          <li>Bootstrapping assertions for multi-agent validation loops.</li>
        </ul>

        <h3 style="font-family: var(--font-heading); font-size: 1.25rem; color: var(--text-primary); margin-bottom: 0.75rem;">Local Installation</h3>
        <p style="color: var(--text-secondary); line-height: 1.6; margin-bottom: 1rem;">Tessera handles eval dataset compilation and rubric testing offline inside your codebase:</p>
        <div class="terminal-box" style="position: relative; background: rgba(0,0,0,0.3); border: 1px solid var(--card-border); padding: 1rem 3.5rem 1rem 1.25rem; border-radius: 6px; font-family: var(--font-mono); font-size: 0.85rem; color: var(--text-primary); max-width: 600px;">
          <span class="terminal-prompt" style="color: var(--accent-cyan); font-weight: bold; margin-right: 0.5rem;">$</span>
          <code class="terminal-cmd">pip install tesserakit-core tesserakit-evals</code>
          <button class="copy-terminal-btn" id="copy-npm-btn" style="position: absolute; right: 0.6rem; top: 50%; transform: translateY(-50%); background: rgba(255,255,255,0.05); border: 1px solid var(--card-border); color: var(--text-secondary); padding: 0.25rem 0.5rem; border-radius: 4px; font-size: 0.68rem; cursor: pointer;">Copy</button>
        </div>
      </div>

      <!-- Constraints & Resources Right -->
      <div class="info-sidebar" style="display: flex; flex-direction: column; gap: 2rem;">
        <div class="sidebar-box card-glass" style="padding: 1.75rem; border-radius: 12px;">
          <h5 style="font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: var(--text-primary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.85rem;">Tool Profile</h5>
          <div class="tool-profile-list">
            <div class="profile-row">
              <span class="profile-label">Status</span>
              <span class="profile-val" style="color: var(--accent-purple); font-weight: bold;">Prototype</span>
            </div>
            <div class="profile-row">
              <span class="profile-label">Mode</span>
              <span class="profile-val">Browser Sample</span>
            </div>
            <div class="profile-row">
              <span class="profile-label">Engine</span>
              <span class="profile-val">Tessera EvalsPack</span>
            </div>
            <div class="profile-row">
              <span class="profile-label">Future Wiring</span>
              <span class="profile-val"><a href="/docs/tool-status/">Tessera evals-pack</a></span>
            </div>
            <div class="profile-row">
              <span class="profile-label">Output</span>
              <span class="profile-val">JSONL + Rubric draft</span>
            </div>
          </div>
        </div>

        <div class="sidebar-box card-glass" style="padding: 1.75rem; border-radius: 12px;">
          <h5 style="font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: var(--text-primary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.85rem;">Browser Limits</h5>
          <p style="color: var(--text-secondary); font-size: 0.88rem; line-height: 1.5; margin: 0;">Online uploads are restricted to 10 rows. Local CLI executions run offline with zero restrictions.</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script is:inline>
  const csvInput = document.getElementById('csv-input');
  const generateBtn = document.getElementById('generate-btn');
  const copyBtn = document.getElementById('copy-json-btn');
  const loadSampleBtn = document.getElementById('load-sample-btn');
  const placeholder = document.getElementById('canvas-placeholder');
  const codeBox = document.getElementById('json-code-box');
  const codeOutput = document.getElementById('json-output');

  const sampleCsv = `query,response,expected_criteria
"What is the system theme?","Sans Serif Systems uses a dark cyber theme.","Must reference dark cyber theme"
"Explain guided loops.","Guided loops use evaluation checks to auto-correct errors.","Should mention eval checks and self-correction"`;

  loadSampleBtn.addEventListener('click', () => {
    csvInput.value = sampleCsv;
  });

  generateBtn.addEventListener('click', () => {
    const rawValue = csvInput.value.trim();
    if (!rawValue) {
      alert('Please paste some CSV data first.');
      return;
    }

    try {
      // Simple client-side CSV parser
      const lines = rawValue.split('\n');
      if (lines.length < 2) {
        alert('Invalid CSV: Needs at least a header row and one data row.');
        return;
      }

      // Simple CSV cell splitter (handles quotes)
      const parseRow = (text) => {
        const matches = text.match(/(".*?"|[^",\s]+)(?=\s*,|\s*$)/g) || text.split(',');
        return matches.map(c => c.replace(/^["']|["']$/g, '').trim());
      };

      const headers = parseRow(lines[0]);
      const jsonlEntries = [];

      for (let i = 1; i < lines.length; i++) {
        if (!lines[i].trim()) continue;
        const row = parseRow(lines[i]);
        const entry = {};
        headers.forEach((h, index) => {
          entry[h] = row[index] || '';
        });
        
        // Structure as a canonical evaluation unit
        jsonlEntries.push(JSON.stringify({
          input: entry.query || entry.input || '',
          output: entry.response || entry.output || '',
          evaluation_rubric: {
            assertions: [
              {
                type: "contains_phrase",
                expected: entry.expected_criteria || entry.expected || "Must provide accurate context"
              }
            ]
          }
        }, null, 2));
      }

      const compiledOutput = jsonlEntries.join('\n');
      codeOutput.textContent = compiledOutput;
      
      placeholder.style.display = 'none';
      codeBox.removeAttribute('hidden');
      copyBtn.style.display = 'block';
    } catch (e) {
      alert('Error parsing CSV: ' + e.message);
    }
  });

  copyBtn.addEventListener('click', () => {
    navigator.clipboard.writeText(codeOutput.textContent).then(() => {
      const origText = copyBtn.textContent;
      copyBtn.textContent = 'Copied!';
      setTimeout(() => {
        copyBtn.textContent = origText;
      }, 1500);
    });
  });

  // CLI copy btn
  const copyNpmBtn = document.getElementById('copy-npm-btn');
  if (copyNpmBtn) {
    copyNpmBtn.addEventListener('click', () => {
      navigator.clipboard.writeText('pip install tesserakit-core tesserakit-evals').then(() => {
        copyNpmBtn.textContent = 'Copied!';
        setTimeout(() => { copyNpmBtn.textContent = 'Copy'; }, 1500);
      });
    });
  }
</script>
